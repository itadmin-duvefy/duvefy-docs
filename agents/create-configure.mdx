---
title: "Create & Configure an Agent"
description: "Step-by-step guide to creating and configuring a Duvefy agent."
---

## Create a new agent

<Steps>
  <Step title="Navigate to Agents">
    In the left sidebar, click **Agents**.

    <Info>
      📸 **Screenshot needed** — `images/agent-create-sidebar.png`

      The left sidebar with the **Agents** item highlighted/active.
    </Info>
  </Step>
  <Step title="Click New Agent">
    Click the **New Agent** button in the top-right corner.

    <Info>
      📸 **Screenshot needed** — `images/agent-create-list.png`

      The Agents list page with the **New Agent** button highlighted in the top-right corner.
    </Info>
  </Step>
  <Step title="Fill in basic info">
    Complete the required fields (see below) and click **Save**.

    <Info>
      📸 **Screenshot needed** — `images/agent-create-form.png`

      The new agent form showing the Name field, Mission textarea, Agent Type dropdown (Donation / Sales), and the Active toggle. Highlight the required fields.
    </Info>
  </Step>
</Steps>

---

## Basic information

### Name
The internal name used to identify this agent in your dashboard. Visitors don't see this name — it's for your team's reference.

### Mission
A short description of the agent's purpose, written in natural language. The AI uses this to understand its role and set the right tone.

**Example:**
> "Help visitors learn about our scholarship programs, answer questions about eligibility, and collect donation pledges."

The clearer and more specific the mission, the better the agent performs.

### Agent type
Select **Donation** or **Sales** based on your primary use case. This shapes how the AI approaches conversations.

### Active toggle
New agents start **inactive**. Turn this on when you're ready for the agent to go live.

<Info>
  📸 **Screenshot needed** — `images/agent-basic-info.png`

  The Basic Info section of a configured agent, showing a filled Name, Mission, agent type set to "Donation", and the Active toggle in the OFF position.
</Info>

---

## Tone & rules

You can optionally add:
- **Tone instructions** — How the agent should communicate (e.g., "Always be warm, empathetic, and encouraging")
- **Rules** — Constraints on agent behavior (e.g., "Never discuss competitor organizations", "Always suggest speaking to a human if the user expresses frustration")

These are appended to the agent's system prompt on every conversation.

<Info>
  📸 **Screenshot needed** — `images/agent-tone-rules.png`

  The Tone & Rules section showing the tone text area with a sample instruction entered, and the rules text area below it.
</Info>

---

## Footer links

Footer links are optional URL pairs shown at the bottom of the chat widget. Useful for directing visitors to important pages.

| Label | URL |
|-------|-----|
| Donate Now | https://example.org/donate |
| About Us | https://example.org/about |

You can add, remove, and reorder links. They appear in the order you set them.

<Info>
  📸 **Screenshot needed** — `images/agent-footer-links.png`

  The Footer Links section showing two or three rows with Label and URL inputs, and the drag handle icon on the left for reordering.
</Info>

---

## Saving changes

Click **Save Changes** at the bottom of the page. Changes to the agent configuration (name, mission, tone, rules, footer links) take effect immediately on your live widget.

<Note>
  Widget styling changes (colors, icon, position) are saved separately in the **Styling** section and update the embed script immediately.
</Note>

---

## Anonymous conversation behavior

When a conversation ends without any CRM record being identified (e.g., a visitor closed the chat before providing their details, or the agent is purely informational), you can control what happens to the transcript.

This setting is available in the **Overview** tab of agent settings, and is only visible when a CRM is connected to your organization.

| Option | Behavior |
|---|---|
| **Discard** (default) | The transcript is not stored anywhere. Deleted from Duvefy when the conversation ends. |
| **Link to catch-all CRM record** | The transcript is attached to a designated CRM record you specify. |

To use the catch-all option:
1. Select **Link to catch-all CRM record**
2. Enter the **CRM object type** (e.g., `Contact`, `Account`, `Leads`)
3. Enter the **CRM record ID** of the specific record to use as the collection point

We recommend creating a dedicated placeholder record in your CRM (e.g., "Anonymous Conversations") rather than using a real contact or account, as it will accumulate many notes over time.
